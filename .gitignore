# venv / окружение
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# SQLite — база не в гите (см. CLAUDE.md), сиды в data/seed/ — в гите
data/*.db
data/*.db-journal
data/*.db-wal

# Streamlit
.streamlit/secrets.toml

# Секреты окружения (CBR_PROXY с логином/паролем и пр.) — НЕ в гит
.env
.env.local
.env.*.local

# IDE / OS
.vscode/
.idea/
.DS_Store
